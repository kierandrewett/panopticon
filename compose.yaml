services:
  panopticon:
    build: .
    ports:
      - "3000:3000"
    environment:
      - TOKEN=${TOKEN}
      - PORT=3000
      - DATA_DIR=/data
      - WEBHOOK_URL=${WEBHOOK_URL:-}
    volumes:
      - data:/data
    restart: unless-stopped

volumes:
  data:
